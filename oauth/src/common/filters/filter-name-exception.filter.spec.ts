import { FilterNameExceptionFilter } from './filter-name-exception.filter';

describe('FilterNameExceptionFilter', () => {
  it('should be defined', () => {
    expect(new FilterNameExceptionFilter()).toBeDefined();
  });
});
